import { handleActions } from 'redux-actions';

export default [] as {
    section: string;
    defaultState: Record<string, unknown>;
    reducers: ReturnType<typeof handleActions>;
    // TODO:
}[];
